<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.by.evgeny07.govore.myhomework">

    <uses-permission android:name="android.permission.INTERNET"/>


    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity android:name=".Menu">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

       <activity android:name=".Dz1">
       </activity>

        <activity android:name=".Dz2">
        </activity>

        <activity android:name=".Dz3">
        </activity>

        <activity android:name=".Dz4_menu">
        </activity>

        <activity android:name=".StartWatch">
        </activity>

        <activity android:name=".StartDiagram">
        </activity>

        <activity android:name=".Owl">
        </activity>

        <receiver android:name=".MyBroadcastResiver" >
            <intent-filter>
                <action android:name="android.net.com.CONNECTIVITY_CHANGE"/>

            </intent-filter>
        </receiver>
<service android:name=".MyServise" android:process=":MYService"/>


    </application>



</manifest>